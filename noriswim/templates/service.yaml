apiVersion: v1
kind: Service
metadata:
  name: {{ include "noriswim.fullname" . }}
  labels:
    {{- include "noriswim.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "noriswim.selectorLabels" . | nindent 4 }}